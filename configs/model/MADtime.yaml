bb_config:
  name: MLPBackbone
  hidden_size: 512
  n_layers: 3

cn_config:
  name: MLPConditioner
  hidden_size: 256
  latent_dim: 128

diff_config:
  name: MADTime
  noise_schedule: std
  norm: on
  pred_diff: on

train_config:
  epochs: 100
  lr: 2.0e-4
  early_stop: 5
  val_step: 1
