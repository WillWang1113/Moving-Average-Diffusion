bb_config:
  name: DiT
  d_model: 128
  patch_size: 8
  num_heads: 8
  # d_mlp: 512 # original 512
  n_layers: 3 # original 3
  dropout: 0.1
  cond_dropout_prob: 0.0
  norm: off
  freq_denoise: off
  # freq_denoise: on

diff_config:
  name: DDPM
  noise_schedule: linear
  norm: off
  pred_x0: off
  T: 1000

train_config:
  epochs: 100
  lr: 2.0e-4
  early_stop: 5
  val_step: 1
  alpha: 1.0e-9
